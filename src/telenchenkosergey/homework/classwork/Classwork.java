package telenchenkosergey.homework.classwork;

// import java.util.Scanner;

public class Classwork {
    public static void main(String[] args) {
        // String str = "London is the capital of Great Britain";
        StringBuilder strBuilder = new StringBuilder();
        strBuilder.append("london");
        System.out.println(strBuilder.reverse().toString());


        // for (int i = 0; i < str.length(); i++) {
        //     System.out.println(strArray[i]);
        // }
        // Scanner input = new Scanner(System.in);

        // int size = 5;
        // String[] strings = new String[5];

        // for (int i = 0; i < size; i++) {
        //     String s = input.nextLine();
        //     strings[i] = s;
        // }

        // input.close();

        // String min = strings[0];
        // String max = strings[0];

        // for (int i = 1; i < strings.length; i++) {
        //     if (strings[i].length() < min.length()) {
        //         min = strings[i];
        //     }
        // }

        // for (int i = 1; i < strings.length; i++) {
        //     if (strings[i].length() > max.length()) {
        //         max = strings[i];
        //     }
        // }

        // System.out.println(min);
        // System.out.println(max);
        // System.out.println(myPhone.getManufacturer());
        // System.out.println(myPhone.getModel());
        // System.out.println(Phone.call("+375293769611"));

        // StringBuilder strBuild = new StringBuilder();
        // strBuild.append("Sergey");
        // strBuild.append(33);

        // String result = strBuild.toString();

        // System.out.println(result);
    }

}
